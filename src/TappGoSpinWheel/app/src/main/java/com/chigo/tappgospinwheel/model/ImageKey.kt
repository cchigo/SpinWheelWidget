package com.chigo.tappgospinwheel.model

import com.chigo.tappgospinwheel.R

enum class ImageKey(val widgetViewId: Int) {
    BG(R.id.img_bg),
    WHEEL(R.id.img_wheel),
    WHEEL_FRAME(R.id.img_frame),
    WHEEL_SPIN(R.id.img_spin_btn)
}